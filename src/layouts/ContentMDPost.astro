---
import BlogBase from "@/layouts/BlogBase.astro";

const { frontmatter } = Astro.props;
---

<BlogBase title={frontmatter.title}>
  <a href="/cool_stuff" class="link-hover no-underline">← Back</a>
  <h1>
    {frontmatter.title}
  </h1>

  <slot />
</BlogBase>
